/*
package com.veterinaria.veterinariaback.util;

import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Spy;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;

class AuditoriaTest {

    @Spy
    @InjectMocks
    private Auditoria auditoria;



    @Test
    void setCodigo() {
        Auditoria auditoria = new Auditoria();

        String cdestado = "Estado";
        String cdusuario = "Usuario";

        auditoria.setCdestado(cdestado);
        assertEquals(cdestado, auditoria.getCdestado());

        auditoria.setCdusuario(cdusuario);
        assertEquals(cdusuario, auditoria.getCdusuario());

        auditoria.setDtfechacreacion(any());
        assertEquals(any(), auditoria.getDtfechacreacion());

        auditoria.setDtfechamodificacion(any());
        assertEquals(any(), auditoria.getDtfechamodificacion());
    }
}
*/
